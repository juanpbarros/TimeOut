# TimeOut

A simple Python application that allows users to schedule a time for their computer to automatically shut down.

## Features

- Schedule shutdown at a specific time
- Cancel a scheduled shutdown
- Input validation for time format

## Project structure

- `src/` — application source code
- `tests/` — automated tests
- `assets/` — icons and visual assets

## How to run

```bash
python src/main.py